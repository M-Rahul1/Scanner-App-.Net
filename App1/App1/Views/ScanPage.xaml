<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App1.Views.ScanPage"
             Title="Scan">
    <StackLayout Padding="10">
        <!-- Entry Field for 16-digit ID -->
        <Entry x:Name="IdEntry" Placeholder="Enter 16-digit ID" Keyboard="Numeric" MaxLength="16" />

        <!-- Button to Simulate QR Code Scanning -->
        <Button Text="Scan QR Code" Clicked="OnScanQRCode" />

        <!-- ListView to Display Scanned IDs -->
        <ListView x:Name="IdListView" HeightRequest="150">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Horizontal Buttons for Actions -->
        <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
            <Button Text="Clear" Clicked="OnClear" HorizontalOptions="FillAndExpand" />
            <Button Text="Delete" Clicked="OnDelete" HorizontalOptions="FillAndExpand" />
        </StackLayout>

        <!-- Action Buttons -->
        <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
            <Button Text="Submit" Clicked="OnSubmit" HorizontalOptions="FillAndExpand" />
            <Button Text="Reload" Clicked="OnReload" HorizontalOptions="FillAndExpand" />
            <Button Text="Next" Clicked="OnNext" HorizontalOptions="FillAndExpand" />
        </StackLayout>

        <!-- Label for Count -->
        <Label x:Name="CountLabel" Text="Count: 0" HorizontalOptions="Center" FontSize="Medium" />
    </StackLayout>
</ContentPage>
